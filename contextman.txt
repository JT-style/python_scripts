1. открыть файл
    file = open("stih.txt')

                                МЕТОДЫ для СЧИТЫВАНИЯ

    read -> file.read() > file.read(3) - считает первые три символа(включая пробел), каретка сдвинется на 3
    seek -> file.seek() - сдвиг каретки на указанную позицию -> file.seek(0) - вернется в начало
    readline -> file.readline() - считает одну строчку(тоже автоматически запоминает где остановилась каретка)
    
file = open('stih.txt', encoding = 'utf-8')

for row in file:  # row -это одна строка
    for lstter in row: # s - один символ в строке
        print(s)

READLINES - метод
    lst = file.readlines() - создаст список элемент которого это строка файла



                        МЕТОДЫ для ЗАПИСЫВАНИЯ
По умолчанию какждый фал открывается в режиме чтения, чтобы записать инормацию в файл нужно поставить его в режим записи.

file = open("stih.txt", 'w', encoding = 'utf-8')
file.write('hello') - мы не допишем в конец файла а перезапишем весь файл, теперь вместо того что мы имели будем иметь только 'hello' в файле

ЧТОБЫ ДОПИСЫВАТЬ В ФАЙЛ НУЖНО ОТКРЫТЬ ФАЙЛ В РЕЖИМЕ ДОЗАПИСИ - a
file = open('stih.txt', 'a', encoding = 'utf-8')

ЧТОБЫ И ЧИТАТЬ И ЗАПИСЫВАТЬ В ФАЙЛ НУЖНО ОТКРЫТЬ ФАЙЛ В РЕЖИМЕ - a+
file = open('stih.txt', 'a+', encoding = 'utf-8')

ФАЙЛ ОБЯЗАТЕЛЬНО НУЖНО ЗАКРЫВАТЬ
file.close()



                            МЕНЕДЖЕР КОНТЕКСТА -(закрывать файл не нужно, он закроется автоматически)


with open("file.txt") as f:
    f = f.readlines().split()
    f = [line.rstrip() for line in f]
    for i in f:
        print(f)
    
